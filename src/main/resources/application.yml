eureka: 
  dashboard:
    path: /eurekaui
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false 


---
spring: 
  cloud: 
    config:
      server:  
        git:
          uri: https://github.com/rksharma1401/spring-config # default 
          search-paths:
            - userApp,spring-cloud-client-app
  

